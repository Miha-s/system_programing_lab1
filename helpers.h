static const int max_word_length = 31; // including terminating zero

int is_character(wchar_t c);

int is_vowel(wchar_t c);

void print_error_and_exit(const char *message);

int max(int left, int right);